{
  "name": "churrasquin",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:calc": "npm run build --workspace=packages/calculator",
    "build": "npm run build:calc && npm run build --workspace=apps/api && npm run build --workspace=apps/web",
    "test": "npm run build:calc && npm run test --workspace=packages/calculator && npm run test --workspace=apps/api && npm run test:e2e --workspace=apps/api",
    "dev:api": "npm run build:calc && npm run start:dev --workspace=apps/api",
    "dev:web": "npm run build:calc && npm run dev --workspace=apps/web"
  }
}
