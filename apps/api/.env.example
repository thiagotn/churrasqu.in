# Postgres do docker-compose da raiz (porta 5433 no host)
DATABASE_URL="postgresql://churrasquin:churrasquin@localhost:5433/churrasquin"
# Troque em produção (Secret no k8s)
JWT_SECRET="dev-secret-troque-em-producao"
